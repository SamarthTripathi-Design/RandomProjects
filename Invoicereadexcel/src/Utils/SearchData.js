import React from "react";

const SearchData = (criteria, data) => {
  let rowData = [];
  console.log(criteria.TmsFileNumber);
  console.log(data[0]);

  return <div>SearchData</div>;
};

export default SearchData;
